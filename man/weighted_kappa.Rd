% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{weighted_kappa}
\alias{weighted_kappa}
\title{Weighted Cohen's Kappa}
\usage{
weighted_kappa(y, predictions, M, type = "quadratic")
}
\arguments{
\item{y}{Integer vector of true class labels.}

\item{predictions}{Integer vector of predicted class labels (same length as
\code{y}).}

\item{M}{Integer.  Number of outcome classes.  Required because not all
classes may be observed in \code{y} or \code{predictions}.}

\item{type}{Character.  \code{"quadratic"} (default) or \code{"linear"}.}
}
\value{
A single numeric value, or \code{NA} if the denominator is zero (e.g.,
when one rater is constant).
}
\description{
Computes Cohen's weighted kappa for ordered classifications.  The weight
matrix entry \eqn{W_{ij}} measures the disagreement between classes \eqn{i}
and \eqn{j}:
\itemize{
\item \strong{Quadratic} (default): \eqn{W_{ij} = (i - j)^2 / (M - 1)^2}
\item \strong{Linear}: \eqn{W_{ij} = |i - j| / (M - 1)}
}
}
\examples{
y    <- c(1L, 2L, 3L, 1L, 2L)
yhat <- c(1L, 2L, 2L, 1L, 3L)
weighted_kappa(y, yhat, M = 3)

}
