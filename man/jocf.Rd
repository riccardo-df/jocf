% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jocf.R
\name{jocf}
\alias{jocf}
\title{Fit a Joint Ordered Correlation Forest}
\usage{
jocf(
  Y,
  X,
  num.trees = 2000L,
  min.node.size = 5L,
  max.depth = NULL,
  sample.fraction = 0.5,
  mtry = NULL,
  splitting.rule = "simple",
  tune.parameters = "none",
  tune.num.trees = 50L,
  tune.num.reps = 100L,
  tune.num.draws = 1000L,
  honesty = FALSE,
  num.threads = NULL,
  ...
)
}
\arguments{
\item{Y}{Integer vector with values in \verb{\{1, ..., M\}} giving the ordered
discrete outcome.}

\item{X}{Matrix or data.frame of covariates (n x k). Columns may be numeric,
factor (ordered or unordered), or logical.  Unordered factor levels are
internally sorted by \code{mean(Y)} and encoded as integer codes; ordered factors
use their existing level order.  Character columns are not accepted; convert
to factor first.}

\item{num.trees}{Positive integer. Number of trees. Default \code{2000}.}

\item{min.node.size}{Positive integer. Minimum number of observations in
a terminal node. Default \code{5}.}

\item{max.depth}{Positive integer or \code{NULL}. Maximum tree depth. \code{NULL}
(default) grows trees without depth constraint. \code{max.depth = 1} produces
stumps (a single split).}

\item{sample.fraction}{Numeric in \verb{(0, 1]}. Fraction of observations to
draw \strong{without replacement} for each tree. Default \code{0.5}.}

\item{mtry}{Positive integer or \code{NULL}. Number of features to consider at
each split. Default \code{NULL} uses \code{floor(sqrt(ncol(X)))}.}

\item{splitting.rule}{Character, one of \code{"simple"} (default) or
\code{"weighted"}. \code{"simple"} uses equal weights across classes;
\code{"weighted"} standardises each class by its population-level Bernoulli
variance (see \code{vignette("jocf")}).}

\item{tune.parameters}{Character. \code{"none"} (default) disables tuning.
\code{"all"} tunes \code{mtry}, \code{min.node.size}, and \code{sample.fraction}.
Alternatively, a character vector naming which parameters to tune
(any subset of \code{c("mtry", "min.node.size", "sample.fraction")}).
Tuning follows the GRF approach: many small mini-forests are evaluated
at random parameter draws, a Kriging surrogate is fitted, and the
parameters with the lowest predicted debiased OOB error are selected
before growing the full forest.  Requires the \pkg{DiceKriging} package.}

\item{tune.num.trees}{Positive integer. Number of trees per mini-forest
during tuning. Default \code{50}.}

\item{tune.num.reps}{Positive integer. Number of random parameter draws
to evaluate with mini-forests. Default \code{100}.}

\item{tune.num.draws}{Positive integer. Number of new random candidates
evaluated via the Kriging surrogate. Default \code{1000}.}

\item{honesty}{Logical. Must be \code{FALSE}; \code{TRUE} is not yet implemented.}

\item{num.threads}{Positive integer or \code{NULL}. Number of OpenMP threads to
use when growing trees and computing in-sample predictions. \code{NULL}
(default) uses all available cores.}

\item{...}{Currently unused.}
}
\value{
An object of class \code{"jocf"} — a list with components:
\describe{
\item{\code{predictions}}{(n x M) matrix of in-sample predicted class
probabilities. Rows sum to 1 and are non-negative.}
\item{\code{classification}}{Named list with two integer vectors (values in
\verb{1, ..., M}):
\describe{
\item{\code{prob}}{Probability-based classification: argmax of forest-
averaged probabilities (see \code{vignette("jocf-theory")}).}
\item{\code{vote}}{Majority-vote classification: each tree votes for its
leaf argmax, then the class with the most votes wins.  Unique to
the unified OCF (see \code{vignette("jocf-theory")}).}
}}
\item{\code{forest}}{List of B tree structures; used by \code{\link[=predict.jocf]{predict.jocf()}}.}
\item{\code{splitting.rule}}{Character; the splitting rule used.}
\item{\code{sample.fraction}}{Numeric; the subsample fraction used.}
\item{\code{M}}{Integer; number of outcome classes.}
\item{\code{num.trees}}{Integer; number of trees grown.}
\item{\code{k}}{Integer; number of training covariates.}
\item{\code{tuning.output}}{\code{NULL} when tuning is off, otherwise a named list
with components \code{status} (\code{"tuned"}, \code{"default"}, or \code{"failure"}),
\code{params} (selected parameter values), \code{error} (debiased OOB error),
and \code{grid} (data frame of evaluated draws and errors).}
\item{\code{call}}{The matched call.}
}
}
\description{
Grows a single random forest whose splitting criterion jointly minimises
average estimation error across all M outcome classes simultaneously.
The criterion is equivalent to average CART Gini impurity (Proposition 1
in the package vignette).
}
\examples{
## Simulate ordered outcome data
set.seed(42)
n <- 150
X <- matrix(rnorm(n * 3), ncol = 3)
Y <- sample(1:3, n, replace = TRUE, prob = c(0.3, 0.5, 0.2))

## Fit a joint ordered correlation forest
fit <- jocf(Y, X, num.trees = 50)

## In-sample predicted probabilities (n x M matrix)
head(fit$predictions)

## Classifications
table(fit$classification$prob)   # probability-based
table(fit$classification$vote)   # majority-vote

## Factor covariates
X_mixed <- data.frame(x1 = rnorm(n), x2 = factor(sample(c("A","B"), n, TRUE)))
fit2 <- jocf(Y, X_mixed, num.trees = 50)
head(fit2$predictions)

\donttest{
## Built-in hyperparameter tuning (requires DiceKriging)
if (requireNamespace("DiceKriging", quietly = TRUE)) {
  fit_tuned <- jocf(Y, X, num.trees = 50,
                    tune.parameters = "all",
                    tune.num.trees = 10,
                    tune.num.reps = 20,
                    tune.num.draws = 50)
  fit_tuned$tuning.output$params
}
}

}
