% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/factor_encoding.R
\name{encode_factors}
\alias{encode_factors}
\title{Encode factor, ordered factor, and logical columns to numeric codes}
\usage{
encode_factors(X, Y = NULL, factor_info = NULL)
}
\arguments{
\item{X}{A data.frame or matrix of covariates.}

\item{Y}{Integer vector of outcomes (training mode only). Used to order
unordered factor levels by \code{mean(Y)}.}

\item{factor_info}{List returned by a previous \code{encode_factors()} call
(prediction mode). \code{NULL} in training mode.}
}
\value{
A list with components:
\describe{
\item{\code{X_encoded}}{Numeric matrix with the same dimensions as \code{X}.}
\item{\code{factor_info}}{Named list (one entry per column, \code{NULL} for numeric
columns) storing the encoding metadata. \code{NULL} when \code{X} had no
non-numeric columns.}
}
}
\description{
In \strong{training mode} (\code{Y} supplied, \code{factor_info = NULL}), detects non-numeric
columns in \code{X}, converts them to integer codes, and returns the encoding
metadata so it can be reapplied at prediction time.
}
\details{
In \strong{prediction mode} (\code{factor_info} supplied), re-encodes \code{X} using the
stored level orderings from training.
}
\keyword{internal}
