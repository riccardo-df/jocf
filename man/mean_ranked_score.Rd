% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{mean_ranked_score}
\alias{mean_ranked_score}
\title{Mean Ranked Probability Score (RPS)}
\usage{
mean_ranked_score(y, predictions, use.true = FALSE)
}
\arguments{
\item{y}{Integer vector with values in \verb{\{1, ..., M\}}, or an (n x M) matrix
of true probabilities when \code{use.true = TRUE}.}

\item{predictions}{(n x M) numeric matrix of predicted probabilities.}

\item{use.true}{Logical.  If \code{TRUE}, \code{y} is interpreted as a matrix of true
conditional probabilities rather than observed labels.  Default \code{FALSE}.}
}
\value{
A single numeric value.
}
\description{
Computes the Ranked Probability Score, which is sensitive to the ordering of
classes.  Defined as
\deqn{\textrm{RPS} = \frac{1}{M-1} \cdot \frac{1}{n}
  \sum_i \sum_{m=1}^{M-1} \bigl(F_{\textrm{pred}}(m) - F_{\textrm{true}}(m)\bigr)^2}
where \eqn{F(m) = \sum_{j=1}^{m} p_j} is the CDF.
}
\examples{
set.seed(1)
Y <- sample(1:3, 50, replace = TRUE)
P <- matrix(1/3, nrow = 50, ncol = 3)
mean_ranked_score(Y, P)

}
